package com.tripflow.backend.beans;


//@DataJpaTest
//@AutoConfigureTestDatabase(replace = NONE)
//@ActiveProfiles("ci")
public class BaseEntityProxyIT {
//	@Autowired
//    private EntityManager entityManager;
//
//    @Test
//    void hibernateProxyEqualsRealEntityWithSameId() {
//        TestPlace saved = new TestPlace();
//        saved.setExternalPlaceId("abc123");
//        saved = entityManager.persistFlushFind(saved);
//        entityManager.clear(); // detach so getReference returns a proxy, not the managed instance
//
//        TestPlace proxy = entityManager.getReference(TestPlace.class, saved.getId());
//
//        assertThat(proxy).isEqualTo(saved);
//        assertThat(proxy).hasSameHashCodeAs(saved);
//    }
}
